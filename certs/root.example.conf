[req]
default_bits = 4096
prompt = no
default_md = sha256
distinguished_name = req_distinguished_name
req_extensions = v3_req

# 新的证书请求DN定义，更具描述性和灵活性
[req_distinguished_name]
countryName = Country Name (2 letter code)
countryName_default = CN # <--- 使用原 [dn] 的值
stateOrProvinceName = State or Province Name (full name)
stateOrProvinceName_default = Xinjiang # <--- 使用原 [dn] 的值
localityName = Locality Name (eg, city)
localityName_default = Urumqi # <--- 使用原 [dn] 的值
organizationName = Organization Name (eg, company)
organizationName_default = My Company # <--- 使用原 [dn] 的值
organizationalUnitName = Organizational Unit Name (eg, department)
organizationalUnitName_default = IT Department # <--- 使用原 [dn] 的值
commonName = Common Name (eg, fully qualified host name)
commonName_default = My Root CA # <--- 使用原 [dn] 的值，注意这里是为CA本身，如果是叶子证书，则应为grpc.example.com
commonName_max = 64 # <--- 可以添加这个限制

[ca]
default_ca = CA_default

[CA_default]
dir = ./ca
database = $dir/index.txt
new_certs_dir = $dir/certs
certificate = $dir/cacert.pem
serial = $dir/serial
private_key = $dir/private/cakey.pem
default_days = 3650
default_md = sha256
policy = policy_any

[policy_any]
countryName = optional
stateOrProvinceName = optional
localityName = optional
organizationName = optional
organizationalUnitName = optional
commonName = supplied

[v3_ca]
# subjectKeyIdentifier = hash
# authorityKeyIdentifier = keyid:always,issuer
# basicConstraints = critical, CA:true
# keyUsage = cRLSign, keyCertSign

subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid:always,issuer
basicConstraints = critical, CA:true, pathlen:0  # 如果根CA只签发叶子证书，可以添加 pathlen:0
keyUsage = cRLSign, keyCertSign
# 添加 CRL 和 AIA
crlDistributionPoints = URI:http://crl.yourcompany.com/ca.crl # 替换为你的 CRL 地址
authorityInfoAccess = OCSP;URI:http://ocsp.yourcompany.com, CA;URI:http://crl.yourcompany.com/ca.crt # 替换为你的 OCSP 和 CA 证书路径


[v3_req]
basicConstraints = CA:FALSE
keyUsage = digitalSignature, keyEncipherment
extendedKeyUsage = serverAuth, clientAuth
subjectAltName = @alt_names

[alt_names]
DNS.1 = grpc.example.com  # 替换为你的 gRPC 服务端主机名
IP.1 = 127.0.0.1  # 如果你的服务端使用 IP 地址，添加 IP 地址
# 可以添加更多的 DNS 或 IP 地址，例如：
# DNS.2 = another.example.com
# Email.1 = user@example.com  # 如果你的服务端使用电子邮件地址，添加电子邮件地址
